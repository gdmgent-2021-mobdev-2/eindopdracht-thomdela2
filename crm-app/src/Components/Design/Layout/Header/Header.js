import React from 'react';
import LogoutButton from './LogoutButton';
import './Header.css';

const Header = () => {

    return (
        <header>
            <p>header</p>
            <LogoutButton />
        </header>
    )
};

export default Header;