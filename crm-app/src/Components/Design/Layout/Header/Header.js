import React from 'react';
import LogoutButton from './LogoutButton';

const Header = () => {

    return (
        <header>
            <p>header</p>
            <LogoutButton />
        </header>
    )
};

export default Header;